import { Ref } from "vue";
type ImageItemSize = {
    width: number;
    height: number;
};
interface Props<T> {
    items: T[];
    masonryRef: Ref<any>;
    columnsNum: number;
    columnWidth: number;
}
export default function useMasonry<T extends ImageItemSize>({ items, masonryRef, columnsNum, columnWidth }: Props<T>): {
    columns: Ref<{
        height: number;
        items: import("@vue/reactivity").UnwrapRefSimple<T>[];
        index: number;
    }[]>;
    redraw: (columNum: number) => void;
};
export {};
